使用当前 Skill 包中的 Canonical Reference Implementation 生成流体玻璃卡片页面。必须直接复用 `references/fluid-glass-material.css`、两个 canonical shader 和 `references/fluid-glass-engine.js`，不得自行重做 Shader。卡片必须保持 144px 高、30px 圆角、8% 默认玻璃底材、透明 WebGL Canvas、左侧低 Alpha、中右侧青色流体。默认 Cyan 严格使用 #00E7D2 / #3CC8FF / #075F68，不得出现暖黄色或大理石纹。卡片 key 数据驱动，图表默认关闭。保留统一设置、localStorage、导出 HTML、质量档位、鼠标扰动和 CSS fallback。完成后按 ACCEPTANCE_CHECKLIST.md 自检。